---
title: 【绘图】相关性散点图
urlname: scatter-plot-of-correlation
index_img: https://api.limour.top/randomImg?d=2026-04-25 07:58:27
date: 2026-04-25 15:58:27
tags: 绘图
excerpt: 使用散点图加线性拟合探究两个连续变量的相关性
---

## 配置环境
+ [基础编程环境](/pairwise-comparison-heatmap)

## 示例

+ 加载依赖

```R
require(ggplot2)
require(ggpubr)
require(tidyverse)
require(RColorBrewer)
options(repr.plot.width=6, repr.plot.height=12)
RColorBrewer::display.brewer.all(type = "all")
```

+ 读入数据

```R
data <- iris
data  %>% summary
```

+ 选择数据

```R
df <- data
yName <- 'Petal.Length'
ylim <- c(1, 7)
xName <- 'Petal.Width'
subName <- 'Species'
```

+ 绘图

```R
options(repr.plot.width=6, repr.plot.height=5)
ggscatter(
    data = df[c(xName, yName, subName)] %>% na.omit(),
    x = xName,
    y = yName,
    conf.int = T,
    cor.coef = T,
    cor.method = 'pearson',
    color = subName,
    size = 2, 
    alpha = 0.5
  ) +
scale_color_manual(values = RColorBrewer::brewer.pal(3, "Dark2")) + 
stat_smooth(method = 'lm', se = T, color = 'grey') + 
lims(y = ylim) + theme_bw() + 
theme(panel.grid = element_blank(), legend.position = "right")
```

![](https://img.limour.top/2026/04/25/69ec759e0ac71.webp)