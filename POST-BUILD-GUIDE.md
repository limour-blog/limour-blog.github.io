# 新文章构建指南（每次写新文章时参考）

> 本文档基于 2026-08-09 撰写「糖果逻辑题」文章（commit `51241ba`）的经验总结，
> 用于指导后续新建文章时的 front matter 配置、正文组织、代码展示等规范。

---

## 1. 新建文章流程

用 `hexo new`（若 PATH 无 hexo，用 `node_modules/.bin/hexo`）创建：

```bash
./node_modules/.bin/hexo n "2026-08-09-【探索】糖果逻辑题"
```

- 文件名/标题前缀即文章类别（见 TAGS-SYSTEM-GUIDE.md §2.4），如 `观点` `故事` `探索` 等。
- 创建后会自动生成 `source/_posts/YYYY-MM-DD-标题.md`。

## 2. front matter 规范（创建后立即修改）

```yaml
---
title: 【探索】糖果逻辑题        # 去掉文件名里的日期前缀 2026-08-09-
urlname: candy-logic-puzzle      # 纯英文小写，空格用 - 替换（决定永久链接，勿随意改）
index_img: https://api.limour.top/randomImg?d=2026-08-09 04:45:06
date: 2026-08-09 12:45:06
tags: 探索                        # tags 与文章类别相同即可
excerpt: 一句话概括               # 正文写完后补一句摘要
---
```

- `title` 去掉 `YYYY-MM-DD-` 前缀。
- `urlname`：纯英文小写 + `-` 替换空格（中文标题用英文意译，如「糖果逻辑题」→ `candy-logic-puzzle`）。
- `tags`：与文章类别一致（单标签 `tags: 探索`；多标签 `tags: [a, b]`，见 TAGS-SYSTEM-GUIDE）。
- `excerpt`：正文写完后，用一句话概括文章内容。

## 3. 正文组织要点（经验教训）

### 3.1 先确认文章「目的」

- 写之前先明确：这篇是**介绍题目**，还是**介绍自己的脚本/工具**？两者篇幅与侧重点完全不同。
- 例：糖果题文章最初误写成介绍题目，用户纠正「目的不是介绍一道题，是介绍我写的脚本」后才改对。
- **先问清楚主旨，再动笔**，避免推倒重写。

### 3.2 信息密度要高，不要来回绕

- 用户反馈：`文字内容信息密度高一点，不要咕噜话来回说`。
- 避免把同一件事用不同的话重复讲三遍；每段只传达一个有效信息。
- 能用表格/列表/代码表达的就别用长句反复解释。

### 3.3 展示代码：完整 + 折叠

- **不要省略代码**：用户明确「不许省略」。放 `...` 占位会被打回。
- 长代码用 fold 折叠块收起，避免正文过长：

{% fold primary @candy_puzzle.py 完整源码 %}
```python
...完整代码...
```
{% endfold %}

- fold 语法：`{% fold <样式> @<标题> %} ... {% endfold %}`，中间支持 Markdown。
- 折叠块里的代码必须**完整可运行**，写完用 `ast.parse`/直接运行验证语法。

### 3.4 代码块的验证

- 从文章里提取代码块运行测试，确认语法 OK、能正常输出，再提交。
- 例：糖果题脚本折叠块提取后 `python3 -c "ast.parse(...)"` 通过，且能渲染出题目。

## 4. 常见坑

- `excerpt` 留空没关系，但正文写完记得补一句话摘要。
- 折叠块里的中文引号直接写 `“”` 即可，无需用 `\u201c` 转义（会显得不自然）。
- 代码里的注释/中文在 markdown 代码块中无解析问题，直接中文即可。
- 参考 `~/pi-test/` 下的脚本时，先读脚本再理解其设计，别凭猜测写介绍。

## 5. 命令速查

```bash
./node_modules/.bin/hexo n "标题"          # 新建文章（PATH 无 hexo 时用相对路径）
npx hexo g                                 # 构建；无 FATAL 即成功
npx hexo s -p 3000                         # 预览 http://localhost:3000/
npx hexo deploy                            # 部署到 gh-pages
```

> 写完文章后按 DEPLOY-CHECK.md 走「人工检查 → 部署确认」流程。