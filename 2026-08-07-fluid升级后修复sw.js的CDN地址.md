# 【记录】fluid 升级后修复 sw.js 的 CDN 地址

> 记录时间：2026-08-07

博客主题从 fluid 1.9.8 升级到 1.9.9 后，发现 Waline 评论无法初始化。排查后发现是 [sw.js 实现的 CDN 高可用方案](/service-worker-resource-high-availability.html) 与新版主题的资源加载方式冲突，借此机会把主题全部可能从 CDN 加载的资源都收编进了 sw.js，并修掉了一批历史遗留的错误。

## 根因一：waline 加载方式变了

对比两个版本的 `waline.ejs`：

| 版本 | 加载方式 | 需要的构建 |
|---|---|---|
| fluid 1.9.8 | `<script>` 经典加载 + 全局 `Waline.init()` | UMD |
| fluid 1.9.9 | `<script type="module">` + `import('waline.js').then(({init}) => init(options))` | ESM 具名导出 `init` |

而旧 sw.js 里有一条历史补丁，会把 `waline.js` 重写为 `waline.umd.js`：

```js
const cdn_patch = [
	[/waline.js$/, "waline.umd.js"]
];
```

这条规则在 1.9.8 时代是正确的（经典 script 需要 UMD 挂全局），但 1.9.9 改用 `import()` 后，UMD 文件被当作模块加载时**没有任何具名导出**，`({ init })` 解构出 `undefined`，`init(options)` 直接抛错。

关键事实：`@waline/client` 的 `dist/waline.js` **本身就是 ESM 构建**（package.json 中 `exports['./full'].import = './dist/waline.js'`，文件以 `export { ... as init ... }` 结尾），所以正确做法是**删掉这条 patch，让 waline.js 原样通过**。

```js
// 修复：移除 cdn_patch
const cdn_patch = [];
```

## 根因二：顺手揪出的历史错误

逐条 curl 验证 sw.js 里所有 CDN 地址时，发现以下问题：

| 问题 | 原因 | 修复 |
|---|---|---|
| 复制按钮可能失效 | `clipboard-js@0.3.6` 是**另一个库**（没有 `ClipboardJS` 类），只有 baomitu 的 `clipboard.js/2.0.11` 才是主题用的 | npm 源改用 `clipboard@2.0.11/dist/` |
| anchor 版本漂移 | jscdn/jsdelivr 固定 4.3.1，主题默认 5.0.0 | 统一 5.0.0 |
| katex 版本不一致 | jscdn/jsdelivr 是 0.16.21，baomitu/sustech 是 0.16.2 | 统一 0.16.2（cdnjs 没有 0.16.21） |
| waline 版本漂移 | 固定 3.5.2，主题默认 3.6.0 | 统一 3.6.0，并把 4 个源改为与测速索引对齐的 `[jscdn, jsdelivr, baomitu, sustech]` |

> 顺带一提：原 `cdn_list["waline"]` 是 `[jscdn, jsdelivr, unpkg, zstatic]`，而测速 `cdn_index` 返回的 id 对应 `[jscdn, jsdelivr, baomitu, sustech]`，索引错位——若测速最快的是 baomitu/sustech，waline 会被导向 unpkg/zstatic，"测速选优"实际不生效。这次一并对齐。

## 全覆盖：穷举主题的 static_prefix

用 grep 穷举主题所有 `static_prefix` 引用，把还没走 sw.js 的资源全部收编：

```bash
grep -rhoE "static_prefix\.[a-z_]+" node_modules/hexo-theme-fluid/layout/ | sort -u
```

新增 11 个 key：`prismjs`、`typed`、`fancybox`、`mathjax`、`mermaid`、`valine`、`gitalk`、`disqusjs`、`twikoo`、`discuss`、`moment`，并在 `_config.fluid.yml` 中把对应 `static_prefix` 改为 `/cdn/<key>/`。现在主题的全部静态资源（除 busuanzi 计数服务、alicdn 图标字体外）都经 sw.js 按连通性选最快 CDN。

## 各 CDN 路径差异的坑

sw.js 的机制是替换 `/cdn/<key>/` 前缀、保留请求路径后缀，因此**每个源的目录结构必须与主题请求的文件名一致**，逐一验证时踩到这些坑：

- **typed**：npm 包结构是 `lib/typed.min.js`，而 baomitu/cdnjs 在根目录放 `typed.min.js`，无法用同一后缀兼容 4 个源 → 只列 baomitu + sustech
- **fancybox**：npm 上的包名是 `@fancyapps/fancybox@3.5.7`（`fancybox@3.5.7` 不存在），路径 `dist/` 下才有 `jquery.fancybox.min.js`
- **discuss**：cdnjs 没有该包 → 只列 jscdn、jsdelivr、baomitu
- **waline**：ESM 版文件名就是 `waline.js`，不要写 `waline.mjs`（3.5.2 的 dist 里根本没有 mjs）也不要写 `waline.umd.js`

## 健壮性改进

部分 key 只有 2~3 个源，而测速索引固定返回 0~3，越界会拿到 `undefined` 导致 URL 变成 `"undefined"`。增加兜底：

```js
const newUrl = url.href.replace(cdn_regex, list[await cdn_index] || list[0]);
```

未知 key（比如以后新增插件）不再让请求挂起，直接走默认请求：

```js
const list = cdn_list[key];
if (!list) { resolve(fetch(request)); return; }
```

## 验证方法

1. 每个 CDN URL 逐个 `curl -o /dev/null -w "%{http_code}"` 验证 200（含 waline 的 ESM 内容检查：`grep export`）
2. `node --check source/sw.js` 语法检查
3. `npx hexo generate` 后比对生成页面里的 `/cdn/<key>/` 与 sw.js 的 `cdn_list` 键是否一一对应
4. 生成页面确认 waline 以 `import('/cdn/waline/waline.js')` 加载

排查结论：**所有 CDN 文件地址其实都还活着（全部 200），失效的是"逻辑地址"——一条过时的重写规则撞上了主题新的模块化加载方式**。升级主题后，遇到资源加载异常，优先对比新旧版本主题的资源加载代码，而不是急着换 CDN。
